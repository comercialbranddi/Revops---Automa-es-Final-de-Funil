# Pipedrive — leitura do pipeline 25 (Eventos)
PIPEDRIVE_API_TOKEN=

# Supabase do projeto Lia (rpqfxrmqsgiqzkroxemk) — fluxo ao vivo, erros
# abertos/resolvidos, placar e sorteio. As tabelas de evento têm RLS ligado e
# nenhuma policy pra `anon`, então precisa ser a SERVICE ROLE KEY
# (Supabase → Settings → API → service_role). Ela só é lida em route handlers
# (server-side) e nunca chega ao browser. Sem ela o painel continua de pé,
# mas só com o que vem do Pipedrive.
SUPABASE_URL=https://rpqfxrmqsgiqzkroxemk.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
